<section class="text-classic">
    <div class="container">
        <div class="text-classic__inner">
            <h1 class="text-classic__title">{{ title|default('') }}</h1>
            <div class="text-classic__content">{{ content|default('') }}</div>
        </div>
    </div>
</section>