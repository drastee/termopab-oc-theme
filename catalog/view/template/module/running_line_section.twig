<section class="running-line">
    <div class="running-line__wrapper">
        <div class="running-line__marquee">
            <span>{{ text }}</span>
            <span>{{ text }}</span>
            <span>{{ text }}</span>
            <span>{{ text }}</span>
            <span>{{ text }}</span>
            <span>{{ text }}</span>
        </div>
    </div>
    <img src="{{ image }}" alt="">
</section>
